/* * * ./app/comments/model/comment.ts * * */
export class AlumniUserProfile {
    constructor(
        public id: string, 
        public description: string, 
        public title:string
        ){}
}